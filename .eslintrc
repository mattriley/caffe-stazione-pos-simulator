{
    "extends": ["react-app"], // "shared-config"],
    "rules": {
      // "additional-rule": "warn"
    },
    "overrides": [
      {
      "files": ["**/*.js?(x)"],
        "rules": {
          "react-hooks/rules-of-hooks": "warn"
        }
      }
    ]
}
